const campoNome = document.getElementById("nome");
const formylario = document.getElementsByTagName("form");
const botaoEnviar = document.getElementById("btn-enviar");

botaoEnviar?.addEventListener("click", (e: MouseEvent) => {
    e.preventDefault();
    e.pageX;
    e.pageY;
});

// botaoEnviar?.addEventListener("click", (e: KeyboardEvent) => {
//     e.preventDefault();
//     e.pageX;
//     e.pageY;
// })